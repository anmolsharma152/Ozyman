# InsForge project credentials & CLI state (never commit)
.insforge

# AI agent skill / config dirs
.agent
.agents
.augment
.claude
.cline
.github/copilot*
.kilocode
.qoder
.qwen
.roo
.trae
.windsurf

# Secrets & local env (never commit real keys)
.env
.env.local
.env*.local
!.env.example

# Dependencies & build output
node_modules
.next
dist

# OS
.DS_Store
